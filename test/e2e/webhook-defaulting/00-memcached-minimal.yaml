apiVersion: memcached.c5c3.io/v1alpha1
kind: Memcached
metadata:
  name: test-webhook-defaults
spec:
  # Only resources are specified — all other fields should be defaulted by the webhook.
  resources:
    requests:
      cpu: "50m"
      memory: "64Mi"
    limits:
      cpu: "100m"
      memory: "128Mi"
